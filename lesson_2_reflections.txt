What happens when you initialize a repository? Why do you need to do it?
The information structure is created. To start tracking changes. 

What are some situations when branches would be helpful in keeping your history organized? How would branches help?
When splitting the mail line into different versions. Eg. light and full version. The base code is the same, but additional fetures are not. 

How do the diagrams help you visualize the branch structure?
The commit structure is like a tree. The tree diagram shows the structure of the branching in the code. 

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?
The code merges into one line and the diagram shows the two paths merge. 